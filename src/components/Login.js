import React from 'react'

const Login = () => {
  return (
    <form onSubmit={undefined}>
      <input value={undefined} name="username" type="text onChange{}" />
      <input value={undefined} name="password" type="text onChange{}" />
      <input type="submit" value="Log In" />
    </form>
  )
}

export default Login